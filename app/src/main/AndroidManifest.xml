<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gcoders.pokemon.pokemonguide.pokemonguide">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@drawable/ic_pokemon_launcher"
        android:roundIcon="@drawable/ic_pokemon_launcher"
        android:supportsRtl="true"
        android:name=".PokemonGoApplication"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.gcoders.pokemon.pokemonguide.pokemonguide.view.MainActivity"
            android:label="@string/app_name"
            android:configChanges="orientation|keyboardHidden"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.gcoders.pokemon.pokemonguide.pokemonguide.view.PokemonDetailActivity"/>
        <activity android:name="com.gcoders.pokemon.pokemonguide.pokemonguide.view.InfoActivity"/>
    </application>
</manifest>
